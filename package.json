{
  "name": "fullstack-app",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "dev": "lerna run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.1.4"
  }
}

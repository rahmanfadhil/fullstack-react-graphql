{
  "name": "yarn-app",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.1.4"
  }
}
